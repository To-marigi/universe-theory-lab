# Final-Theory v0.3.7 literature-frontier note — 2026-07-30

## Version-specific primary-source check

The official arXiv record and PDF for Srivastava and Surya,
arXiv:2603.25503, were checked again on 2026-07-30. The available manuscript
remained v1, submitted on 2026-03-26, with 38 PDF pages. The PDF itself carries
the v1 identifier and date. No later version, correction, journal reference,
or DOI was visible on the official record.

The archived version-specific copy remains:

`references/papers/2603.25503v1_srivastava-surya_quantum-bell-causality-qsg.pdf`

The paper proves commutative collapse for TOBC and NTOBC under nonsingular
dynamics. For CPOBC it derives additional relations but explicitly leaves the
general algebra unresolved. Its displayed d=2 inconsistency calculation fixes
the antichain generators to a Pauli-proportional ansatz; it is not a
classification of arbitrary invertible 2 by 2 matrices. Equation (113) prints
`Q_(n+1)`, while its derivation through Eq. (112) and the Appendix example
remain the basis for retaining separate literal and derived source-index
branches in this repository.

Florentino's 2 by 2 matrix-tuple results remain general simultaneous
triangularisation and similarity tools. Berthomieu, Eder, and Safey El Din
remain prior art for exact saturation-ideal algorithms. Neither source proves
any CPOBC chart empty, validates the project's denominator inventory, or
supplies the v0.3.7 unit-ideal certificates.

## Independent v0.3.7 derivation

The v0.3.7 result is not imported from the literature. For the frozen n<=4
strong-operator compiler, the literal system's 2,564 canonical numerators are
partitioned exactly into 2,552 Q5-independent and 12 Q5-dependent expressions.
The 2,552 expressions are exactly the 976-relation common core of the literal
and derived source-index branches.

Exact QQ saturation on the complete R2, R3, R4 d=2 cover resolves 12 S1 and
9 S2 charts, while the S3 scalar stratum is structurally commutative. No Q5 or
R5 variable or commutativity assumption is used. By forward implication, this
proves `[Q_i,Q_j]=0` for `1 <= i < j <= 4` in every solution of the frozen
literal system. Two prime-field campaigns are consistency scouts only.

Separately, direct QQ evaluation of all 1,001 reduced-operator matrix
relations, together with exhaustive static compiler-provenance coverage of
all 2,564 canonical numerators (979 represented relations plus 22
identically-zero path relations), establishes the general scalar-chain
identity on the recorded denominator-open locus. These are two proof
components with shared compiler/backend lineage, not independent
implementations or two actual evaluation routes. This is a project result,
not a claim made by the cited paper.

## Claim boundary and remaining frontier

The result is finite (`n<=4`), fixed-dimensional (`d=2`), nonsingular, and
restricted to the repository's strong operator interpretation. It does not:

- resolve the printed Eq. (113) author intent;
- prove a restriction theorem from the infinite CPOBC algebra to the compiler;
- classify the weak or occurrence-dependent operator profile;
- classify dimensions `d>=3`;
- construct a sigma-algebra extension of a noncommutative quantum measure; or
- turn the literal branch's unconstrained Q5 fiber into evidence for the
  intended infinite CPOBC algebra.

The literature search is necessarily bounded by the official records and
queries visible on 2026-07-30. It supports
`NO_EXTERNAL_RESULT_SUPERSEDES_V037`, not a theorem that no unindexed or future
result exists. The global programme verdict remains `FINAL_THEORY_OPEN`.
