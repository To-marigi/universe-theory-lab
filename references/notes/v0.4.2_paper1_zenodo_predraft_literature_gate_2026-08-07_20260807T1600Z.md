# Research note: Paper I fresh Zenodo predraft literature gate

## Source record

The official arXiv API data are registered as
`arXiv:PaperI-zenodo-predraft-gate-2026-08-07T1600Z`. Raw full-overlap and
exact-ID Atom responses, a retrieval receipt, and a normalized ledger are
preserved under `references/papers/` with independent SHA-256 bindings in
`references/sources.json` and `references/manifest.json`.

The retrieved full-overlap feed cutoff is `2026-08-07T15:52:17Z`. Its fixed
submitted-date query — identical in scope to every prior gate in this chain —
now contains 908 records. The separately retrieved tracked records are
`2603.25503v1` and `2607.26672v1`.

## Comparison and scientific boundary

Against the immutable `2026-08-06T2315Z` predraft snapshot, the newly
archived response is not a zero-delta recheck: the still-closed submittedDate
window accumulated real arXiv search-index catch-up (722 -> 908 records).
Every difference was individually reviewed rather than assumed nonmaterial:

- 200 added versioned IDs, 14 missing (superseded sides of version
  replacements), 14 true version replacements (abstract byte-identical in
  every case, 4 with a clarity-only title reword), 6 new rule-triggered
  candidates never seen by the prior gate.
- No title, abstract, category, or other normalized-metadata delta and no
  screening-decision delta among versioned IDs shared unchanged between the
  two snapshots.
- The 28 rule-triggered candidates already known to the prior gate are
  unchanged in screening decision.

Each of the 6 new candidates and 14 version replacements was read from the
official arXiv API and classified nonmaterial to C1--C5 by title/abstract
review; see the normalizer's `PREDRAFT_REVIEWED_NEW_TRIGGER_RATIONALES` and
`PREDRAFT_REVIEWED_VERSION_UPDATE_RATIONALES` for the individually authored
rationale behind each classification, and the dated report for a readable
summary.

This is a bounded source-data observation, not a project-derived theorem and
not a claim of exhaustive novelty or literature absence. It also does not
authorize any Zenodo action. The full machine-readable comparison, including
every reviewed delta list and its digest, is in
`references/papers/2026-08-07_paper1_zenodo_predraft_20260807T1600Z_arxiv_normalized.json`.

## Implementation consequence

No manuscript or build artifact was changed by this gate. Do not bind a
builder or verifier to this result merely because the technical gate is
closed; owner authorization and the remaining release controls are separate
decisions.
