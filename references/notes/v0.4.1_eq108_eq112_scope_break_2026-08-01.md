# Eq. (108)/Eq. (112) semantic-scope note for v0.4.1

Date: 2026-08-01

## Source facts already preserved in the repository

The archived Srivastava--Surya v1 paper is the version-specific source for
Eqs. (107), (108), (111), and (112).  The existing visual audit records that
Eq. (107) reconstructs non-timid transitions and Eq. (108) gives the timid
inclusion--exclusion formula.  The frozen v0.3.1 dependency graph classifies
Eq. (108) as `MSR_ELIMINATION`, and the v0.3.3 Eq. (112) compiler limits its
forward derivation to `PAPER_STRONG_OPERATOR_PROFILE`.

## Independent project derivation

The v0.4.1 one-sided campaigns reused the v0.3.5 direct/Q artifact, whose
declared semantic profile is `PAPER_STRONG_OPERATOR_PROFILE`.  Relation-family
filtering happened after its Eq. (107)/(108) and Eq. (112) dependency
definitions had been fixed.  Therefore deleting explicit strong-MSR records
did not remove strong-MSR-dependent Eq. (108) coordinates, and deleting
explicit strong-GC records did not remove strong-GC-dependent Eq. (112)
coordinates.

The resulting exact ideals describe restricted loci, not conservative
supersets of the intended one-sided source profiles.  Their emptiness cannot
establish the one-sided semantic theorems.

An exact source-native separation was also constructed for the reachable-MSR
side: `A_t = diag(p_t,1)` with the CSG character `p_t` and
`Omega=(1,0)^T` satisfies all source CPOBC, strong-GC, nonsingularity, and
reachable-state-MSR gates through stage four, while every strong-MSR operator
identity fails.  The first residual is `diag(0,1)` and annihilates `Omega`.
Since the Q reconstruction makes the same early MSR constraint an operator
identity, its image is a proper restricted slice of the target profile.

## Claim boundary

This finding invalidates the v0.4.1 proof interpretation, not the arithmetic
of its 42 certificates and not the underlying one-sided mathematical
statements.  Both one-sided statements are open until a profile-native
compiler or a direct rational witness settles them.  The strong/strong public
result and the v0.4 weak/weak source-coordinate witness are unaffected.
