# v0.3 held-out and mutation audit

Held-out status: `HELD_OUT_PROTOCOL_PARTIAL`.
Mutation status: `CRITICAL_MUTATIONS_PASS` (14/14 killed).

No parameter, ordering rule, probability weight or scientific threshold was
retuned after a held-out result.

## Held-out partitions

| Partition | Held-out item | Result |
|---|---|---|
| Bell family/source size | source \(n=5\), beyond the \(n\leq4\) CPOBC compiler | exact diagnostic coverage; physical Bell map remains undefined |
| Causet size | 195 geometry histories at \(n=5\) | exact PASS |
| Operator dimension | \(d=4\) Weyl points after \(d=3\) controls | no candidate in listed points |
| Jordan stratum | pointwise exact strata | diagnostic only; not stratum-complete |
| Ansatz class | companion-word points | no candidate in listed points |
| Initial state | non-root density operator | blocked by the fixed one-dimensional root |
| Covariant event | every final-causet pair beyond the first witness | exact PASS |
| Kraus representation | exact \(3\)-\(4\)-\(5\) Kraus rotation control | exact channel-invariance PASS |

The blocked initial-state partition and incomplete continuous ansatz/Jordan
strata prevent a blanket held-out PASS. Held-out diagnostics cannot repair the
missing spectator reduction required by Kraus Bell causality.

## Critical mutations

All of the following are detected by at least one exact test or scientific
typing gate:

1. CPOBC product order reversed;
2. precursor-size comparison reversed;
3. equal-size second orientation or Eq. 104 deleted;
4. inverse assumption deleted from a solved form;
5. automorphism/orbit factor omitted;
6. birth-labelled, outcome-orbit and unlabelled histories conflated;
7. Bell comparison made Kraus-list dependent;
8. incoherent outcomes presented as a coherent sum;
9. off-diagonal \(D\) forced to zero without overlap evaluation;
10. arbitrary Kraus decompositions promoted to physical histories;
11. a decoherence functional fabricated from channel-only data;
12. finite-\(n\) boundedness presented as an infinite extension theorem;
13. a finite ansatz-grid failure promoted to a dimension-wide no-go;
14. a floating residual presented as an exact representation.

The complete machine-readable evidence, including the first exact witnesses,
is embedded in `final_theory_bench_v0.3.json`.
