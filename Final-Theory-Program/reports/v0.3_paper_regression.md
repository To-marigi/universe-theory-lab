# v0.3 minimal paper regression

Verdict: `PAPER_REGRESSION_PASS`.

The continuous-parameter Appendix reconstruction requested by the superseded
v0.3 plan is not performed. The regression is deliberately small and checks
only conventions needed by the new work.

## Exact fixtures

- Eqs. 103–106: precursor sets are ordered by cardinality, not inclusion.
- Unequal size: the ordered word
  \(A_nA'_m=A_mA'_n\) and its right-cleared solved form retain multiplication
  order.
- Equal size: both orientations are required and the additional Eq. 104 word is
  present.
- Representative Eq. 119 and Eq. 130 matrices vanish exactly.
- The three-distinct-Pauli Eq. 164 residual is nonzero.
- A representative \(S_1/S_2\) Pauli assignment has all required inverses but
  violates Eq. 163 exactly.
- A separate rational, noncommuting fixture satisfies the selected CPOBC, MSR
  and GC identities; a reordered product is detectably different.

This confirms that the implementation uses the paper's operator ordering and
assumption boundary. It does not re-prove TOBC/NTOBC, classify all
two-dimensional representations, reproduce the Appendix over a rational
function field, or count as a novel scientific result.
