# SR2-V global bottom-MSR Laurent classification

Date: 2026-08-02

Verdict:
`SR2V_BOTTOM_MSR_GLOBAL_CSG_PARAMETERISATION_CERTIFIED_NONTERMINAL`

## 1. Result and semantic placement

For the occurrence-identified weak/weak transverse chart

```text
A_e = [[a_e,x_e],[0,b_e]],  Omega=e_2,
```

the two certified scalar tori have different roles.

- `G_m^49` is the upper, operator-only scalar character `a`. Fixed-vector GC
  and reachable-state MSR do not impose 24 standalone additive equations on
  this character.
- `G_m^29` is the observed bottom character `b` after the operator monomial
  relations and fixed-vector GC. Since every bottom reachable-state component
  is nonzero on the torus, the bottom coordinate of reachable-state MSR is

  ```text
  -1 + sum_e multiplicity(c,e) b_e = 0
  ```

  for each of the 24 sources.

In the original `b_e` coordinates these equations are affine-linear. Under
the primitive map

```text
b_i = product_j s_j^K_ij,  (s_0,...,s_28) in G_m^29,
```

they are Laurent polynomials, not identities and not ordinary linear
polynomials. All 24 are distinct and nonzero. Their exact census is

```text
source stages                 1 / 2 / 3 / 4 = 1 / 2 / 5 / 16
Laurent terms                 145 total
terms per equation            3--8
Laurent exponent range        -1, 0, 1
maximum parameter support     8
after monomial clearing       3 cubics + 21 quadratics
cutoff-external Q5            absent from all 24 equations
```

The system is globally triangular on `G_m^29`. It has 24 pivot coordinates
and five free coordinates

```text
free = (s18,s21,s25,s26,s28),
```

where `s28` is exactly the cutoff-external `Q5` factor. Consequently the full
nonzero additive bottom-MSR locus is smooth, irreducible, rational, and of
dimension five. It is exactly the normalized finite-CSG principal open with
four coupling parameters, times `G_m(Q5)`.

This globally supersedes only the `global_additive_MSR_variety_classified =
False` boundary in the local certificate
`results/v0.4.2_sr2v_additive_msr_laurent.json`. The local exact CSG point,
rank-24 logarithmic Jacobian, minor `-5/2^45`, and tangent split `4+Q5`
remain independent regression data.

## 2. Exact triangular elimination

Write

```text
r=s18,  u=s21,  v=s25,  w=s26,  q=s28.
```

The pivot order is

```text
(24,22,20,11,19,16,15,27,0,4,17,10,2,8,12,9,6,23,7,13,14,5,1,3).
```

The following table gives every cleared equation after the preceding pivots
have been substituted and its unique solution.

| source | pivot | reduced cleared equation | solution |
|---|---:|---|---|
| `p1-0` | `s24` | `ru*s24 + vw*s24 - vw = 0` | `s24=vw/(ru+vw)` |
| `p2-0` | `s22` | `(ru+rw+2vw)*s22 - ru = 0` | `s22=ru/(ru+rw+2vw)` |
| `p2-2` | `s20` | equivalent to `r*s20-w(r+v)=0` | `s20=w(r+v)/r` |
| `p3-000` | `s11` | `r*s11+ru+3rw+3vw-w=0` | `s11=-(ru+3rw+3vw-w)/r` |
| `p3-002` | `s19` | `r*s19+ru+2rw+3vw-w=0` | `s19=-(ru+2rw+3vw-w)/r` |
| `p3-006` | `s16` | `w*s16+ru+2rw+3vw-w=0` | `s16=-(ru+2rw+3vw-w)/w` |
| `p3-024` | `s15` | `r*s15+ru+rw+2vw-w=0` | `s15=-(ru+rw+2vw-w)/r` |
| `p3-026` | `s27` | `r*s27+ru+rw+2vw-w=0` | `s27=-(ru+rw+2vw-w)/r` |
| `p4-0000` | `s0` | `r*s0-(3ru+6rw+r+8vw-4w)=0` | `s0=(3ru+6rw+r+8vw-4w)/r` |
| `p4-0002` | `s4` | `r*s4-(2ru+3rw+r+5vw-3w)=0` | `s4=(2ru+3rw+r+5vw-3w)/r` |
| `p4-0006` | `s17` | same right side as `s4` | `s17=(2ru+3rw+r+5vw-3w)/r` |
| `p4-000e` | `s10` | same right side as `s4` | `s10=(2ru+3rw+r+5vw-3w)/r` |
| `p4-0044` | `s2` | `r*s2-(ru+rw+r+2vw-2w)=0` | `s2=(ru+rw+r+2vw-2w)/r` |
| `p4-0046` | `s8` | same right side as `s2` | `s8=(ru+rw+r+2vw-2w)/r` |
| `p4-0048` | `s12` | same right side as `s2` | `s12=(ru+rw+r+2vw-2w)/r` |
| `p4-004c` | `s9` | same right side as `s2` | `s9=(ru+rw+r+2vw-2w)/r` |
| `p4-004e` | `s6` | same right side as `s2` | `s6=(ru+rw+r+2vw-2w)/r` |
| `p4-00cc` | `s23` | same right side as `s2` | `s23=(ru+rw+r+2vw-2w)/r` |
| `p4-00ce` | `s7` | same right side as `s2` | `s7=(ru+rw+r+2vw-2w)/r` |
| `p4-0888` | `s13` | `r*s13-r+w=0` | `s13=(r-w)/r` |
| `p4-088a` | `s14` | `r*s14-r+w=0` | `s14=(r-w)/r` |
| `p4-088e` | `s5` | `r*s5-r+w=0` | `s5=(r-w)/r` |
| `p4-08cc` | `s1` | `r*s1-r+w=0` | `s1=(r-w)/r` |
| `p4-08ce` | `s3` | `r*s3-r+w=0` | `s3=(r-w)/r` |

There are no hidden exceptional branches in the first two divisions. On a
torus solution,

```text
(ru+vw) s24 = vw,
(ru+rw+2vw) s22 = ru.
```

Both right sides and both pivots are units. Therefore both displayed
coefficients are forced to be units in the quotient. Every later pivot
coefficient is a Laurent monomial unit except the third sequentially reduced
coefficient. Its denominator is the coefficient in the preceding `p2-0`
identity and is therefore already a quotient-ring unit; its numerator is a
Laurent monomial unit. The artifact stores these sequentially reduced
coefficients separately from the actual diagonal of the original `24 by 24`
pivot Jacobian.

With the 24 pivot variables in the order above, the cleared Jacobian is lower
triangular and has determinant

```text
r^21 s22 w (ru+vw)(ru+rw+2vw).
```

Using the first two equations, this determinant is the quotient-ring unit

```text
r^22 u v w^2 / s24.
```

Thus the Jacobian has rank 24 at every torus solution, not only at the
normalized point.

## 3. Identification with normalized finite CSG

Set `t0=1` and define

```text
lambda(varpi,m) = sum_{k=m}^varpi binomial(varpi-m,k-m) t_k,
D_n = lambda(n,0).
```

For an actual transition at source stage `n`, precursor cardinality `varpi`,
and number of precursor maxima `m`, put

```text
b_e = lambda(varpi,m)/D_n.
```

The exact forward map to the selected Laurent coordinates is

```text
s18 = t2/D3,
s21 = 1/D4,
s25 = t1/D3,
s26 = t2/D4,
s28 = q5.
```

Its exact inverse is

```text
t1 = s25*s26/(s18*s21),
t2 = s26/s21,
t3 = -(s18*s21 + 3*s18*s26 + 3*s25*s26 - s26)/(s18*s21),
t4 =  (3*s18*s21 + 6*s18*s26 + s18 + 8*s25*s26 - 4*s26)/(s18*s21),
q5 = s28.
```

The parameter domain is the principal open on which `q5` and every
`lambda(varpi,m)`, for `0<=m<=varpi<=4`, are nonzero. This is exactly the
nonsingular transition domain seen by the finite `n<=4` inventory. An
independent audit enumerated all 15 such lambda factors: every one occurs as
an actual transition numerator or as a stage denominator, with no missing
factor. After applying the inverse map, each is a product or quotient of torus
units. Thus the forward and inverse formulas are regular on exactly the
declared principal opens, rather than only birational at a generic point.

The machine proof of the identification is:

1. Rebuild the 132-by-29 primitive integer kernel `K` and its 29 original
   free-coordinate rows from the scalar-lattice source artifacts.
2. Evaluate the formula above independently for every one of the 165
   occurrences and verify that it is constant on all 131 ON orbits.
3. Assign each `s_j` the CSG value of its corresponding original free row.
4. Verify symbolically over `QQ(t1,t2,t3,t4,q5)` that
   `product_j s_j^K_ij` equals the direct CSG formula for all 131 actual orbit
   coordinates and equals `q5` for the external coordinate.
5. Directly substitute the family into all 843 operator monomial rows, all
   320 fixed-vector-GC monomial rows, and all 24 additive MSR rows. Every one
   of the 1,187 checks vanishes identically.
6. Compose the displayed forward and inverse maps in both directions and
   compare the triangular formulas with the corresponding CSG coordinates.

No floating-point, finite-field, Groebner, Sage, or other heavy solver is
used. A full rebuild takes only a few seconds.

## 4. Canonical digests

```text
semantic artifact
  d7978cac841bf725baa03df99aa661183a5c96367a2979b245b978f7d5dd08a4

24 Laurent equations
  0dbebe6206e2311b3e93fa98a43b9ba8d17490910b8ad63afbd8d6e98b69fd9f

24 monomial-cleared equations
  c1671278f0e12b48eb42d0af7deb4a468bb62d3ebcc01e7f3ac699c9aeac95ef

triangular records
  b682adfdf39e72050c66aa2b874887376cc82e50a39363a462c5f1ff64a28884

CSG parameterisation record
  e5e62838e558ce61fdcc6c2cbe5efbf1f9ce770a42388786a86f08c4a6076490

all 132 coordinate identities
  5eee27b5f7ce09b3bcdef69d591e1f0d38d7d9554b9cbd8e3c880c507c54b7ce
```

## 5. Separate cocycle principal-open candidate

This result classifies the bottom scalar base only. The next transverse
cocycle problem has scalar base

```text
G_m^49(a) x BottomMSR(b),
```

of dimension `49+5=54`, followed by 132 upper-right coordinates `x_e`. The
full homogeneous cocycle ledger has 1,187 rows:

```text
783 CPOBC + 50 Eq.(113) + 10 completed Eq.(139)
+ 320 fixed-vector GC + 24 reachable-state MSR.
```

A separate read-only exact audit reused the existing upper candidate
`basis-plus-20` and the normalized `t_j=1` bottom point. The resulting
1,187-by-132 rational matrix has rank 132. One selected 132-row minor uses

```text
108 CPOBC + 2 Eq.(113) + 2 Eq.(139)
+ 5 fixed-vector GC + 15 reachable-state MSR
```

and evaluates exactly to

```text
-3^19 * 5^5 * 7^2 * 31 / 2^296 != 0.
```

Therefore the formal determinant of that selected universal submatrix is a
nonzero rational function on the joint scalar base. Its nonvanishing defines
a nonempty principal open on which `x=0`, hence all `Q_1,...,Q_4` are diagonal
and commuting. Possible transverse noncommutative solutions are confined to
the determinant-zero hypersurface.

This rank-132 statement is intentionally **not** included in the present
verdict. It should receive a separate certificate that stores the universal
row IDs, the exact base point, selected minor, and direct matrix reconstruction.
It is also separate from the pair-irreducible state-native `D12` manifest,
whose reachable MSR equations are already built into its fixed harmonic
states.

## 6. Scope boundary

The global classification is a scalar projection theorem in the transverse
globally reducible ON branch. It does not impose strong MSR on the upper
`G_m^49` character, solve the upper-right cocycle fibre, certify a commutator
open, alter or solve the state-native pair-irreducible `D12` manifest, cover
arbitrary rank-two state assignments, produce a weak/weak witness, prove a
full-profile obstruction, or issue an SR2-V terminal.

Machine-readable artifact:
`results/v0.4.2_sr2v_bottom_msr_global_csg.json`.

## 7. Reproduction

```powershell
uv run python -m universe_lab.final_theory.sr2v_bottom_msr_global_csg_v042
uv run pytest -q tests/final_theory/test_sr2v_bottom_msr_global_csg_v042.py
```
