# v0.4.2 fixed-vector GC / strong-MSR escape certificate

Status: **`FIXED_VECTOR_GC_STRONG_MSR_ESCAPE_CERTIFIED`**

This audit closes the evidence asymmetry on the 721 side.  It constructs an
exact rational, nonsingular, occurrence-identified ON-quotient point satisfying
the complete frozen source-level CPOBC system, strong operator MSR, and GC on
the fixed initial vector, while violating strong operator GC and the Eq. (112)
reconstruction.

The full 721 commutativity problem remains open.  The witness is diagonal, so
it is an escape from the old coordinate image, not a noncommutative witness.

## Construction

Let \(\Omega=e_1\).  For each transition occurrence \(e\), set

\[
 A_e=\operatorname{diag}(p_e,r_e).
\]

The first character \(p_e\) is classical sequential growth with \(t_j=1\).
The second character is evaluated from the frozen v0.3.2 Eq. (107)/Eq. (108)
reduction map with

\[
 Q_n=3^{-n},\qquad G_c=3^{-\lvert c\rvert},
\]

except for the single perturbation

\[
 G_{p2\text{-}2}=\frac{2}{9}
 \quad\text{instead of}\quad \frac{1}{9}.
\]

No Eq. (112) relation, numerical solve, finite-field scout, or floating-point
calculation is used.  The reduced scalar expressions are evaluated in
\(\mathbb Q\), and the resulting 165 occurrence matrices are then substituted
back into the source equations directly.

## Exact verification

The implementation checked:

- 165 transition occurrences and 131 ON quotient orbits;
- 165/165 nonzero determinants;
- 783/783 denominator-cleared CPOBC word equations;
- 712/712 inverse-containing CPOBC forms;
- 24/24 strong operator MSR constraints;
- 1529/1529 fixed-vector same-endpoint GC equalities.

The strong operator GC check fails on 510 of the 1529 same-endpoint pairs.  The
first failure has endpoint `p3-002` and paths
`lgc-path-73192a6770fa82828f12` and
`lgc-path-fc1eafd06aa0360d6535`, with exact residual

\[
 \begin{pmatrix}0&0\\0&-2/27\end{pmatrix}.
\]

Its action on \(\Omega\) is zero, as required by fixed-vector GC.

## Direct Eq. (112) escape

The frozen `p2-2` reduction is \(G_{p2-2}=B Q_2 B^{-1}\).  Every matrix in the
witness is diagonal and nonsingular, so the right-hand side is exactly \(Q_2\).
Directly,

\[
 G_{p2-2}=\operatorname{diag}(1/4,2/9),\qquad
 Q_2=\operatorname{diag}(1/4,1/9),
\]

and therefore

\[
 G_{p2-2}-B Q_2 B^{-1}
 =\operatorname{diag}(0,1/9)\ne0.
\]

Thus the fixed-vector-GC/strong-MSR source profile is not contained in the
frozen Eq. (112)-based reconstruction locus.  Relation-family ablation in the
already reduced Q coordinates cannot establish a theorem on the complete 721
profile.

## Scope boundary

This certificate proves only the missing semantic escape statement.  Its four
gregarious antichain matrices are

\[
 Q_n=\operatorname{diag}(2^{-n},3^{-n}),\qquad 1\le n\le4,
\]

so they commute.  Consequently the certificate neither proves nor disproves
commutativity for all nonsingular \(d=2\) points in the 721 profile.

Machine-readable certificate:
`results/v0.4.2_fixed_vector_gc_strong_msr_escape.json`
(SHA-256 `7a51348850ea5d5c3c6048d8f9857b9b983c6945e36772c1e35f3c02da19d495`).

Reproducer:
`src/universe_lab/final_theory/fixed_vector_gc_strong_msr_escape_v042.py`.
