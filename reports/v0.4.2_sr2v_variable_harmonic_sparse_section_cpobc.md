# SR2-V variable-harmonic sparse-section raw-CPOBC audit

Date: 2026-08-02
Verdict: **`SR2V_VARIABLE_HARMONIC_TWO_ALPHA_PRINCIPAL_OPEN_CPOBC_UNIT_IDEAL_CERTIFIED_NONTERMINAL`**

## Result

The exact harmonic `k` which escaped the preceding *two scalar row* mechanism
does not extend to a raw-CPOBC point.  Direct substitution into every raw
operator equation gives 911 nonzero scalar residuals.

More strongly, the full principal-open rational section used by that escape
is empty.  Two unused entries in the same selected operator equations kill
the two retained alpha coordinates.  The resulting beta-one, alpha-zero
raw-CPOBC system has exact rank 62 on the 62-dimensional root-zero harmonic
space, so it forces `k=0`; this contradicts the section's nonzero open
condition.  A rank-optimal 62-row minor supplies a solver-free unit-ideal
certificate.

This closes only the indicated sparse section.  It is not an SR2-V terminal.

## Full direct substitution at the escape point

The normalized finite-CSG primary coordinate `h` is fixed.  The secondary
harmonic boundary is

```text
k(p5-0000000) = 25,
k(p5-000008c) = -1,
all other terminal k-values = 0.
```

It has `k(root)=0`, extends harmonically over all 87 endpoints, and is rank
two with `h`.  The predecessor assignment is

```text
all beta_e = 1,
all unlisted alpha_e = 0,
alpha_E = 5/524,
alpha_V = -179/6.
```

The two selected entry `(1,0)` rows still evaluate to zero.  Independent
reconstruction of all 783 raw CPOBC equations from the raw occurrence ledger
then gives

```text
raw operator equations                         783
raw scalar entries                            3132
zero / nonzero scalar entries             2221 / 911
operator equations with a residual             676
operator equations vanishing completely        107
relations with a residual                       542

nonzero entries (0,0)/(0,1)/(1,0)/(1,1)  89/89/663/70
nonzero Eq103 / Eq104 / second-orientation 739/87/85
```

The first nonzero scalar in raw-ledger order is

```text
relation  cpobc-relation-0b2bbe81c6394d603f63
equation  Eq. (103)
entry     (1,0)
value     -89/4.
```

Even the first selected operator equation already supplies an additional
obstruction:

```text
relation  cpobc-relation-ecdf5451a0bcf06c8da5
equation  Eq. (103)
entry     (0,0)
value     -5/2096.
```

Thus, at this fully specialized point,

```text
1 = (-2096/5) R_f[0,0].
```

The complete ordered nonzero-residual ledger is frozen in the JSON artifact;
its digest is
`d10b502d7c23434f5f761563dd509847d4c1928f87107e35b025591ee47c5eeb`.

## Two extra rows on the rational section

Let the 62 root-zero terminal coordinates be `z_1,...,z_62`.  Specialize all
betas to one and all alphas to zero except

```text
x = alpha_E,
y = alpha_V.
```

The predecessor selected rows have the exact form

```text
F0(k) + Cf(k) x = 0,
G0(k) + Cg(k) y = 0.
```

Their rational section is defined on

```text
Delta(k) = Cf(k) Cg(k) != 0,
x = -F0/Cf,
y = -G0/Cg.
```

At the explicit escape, `(F0,Cf,G0,Cg,Delta)` is

```text
(-5/8, 131/2, -179/2, -3, -393/2).
```

This principal open lies in the rank-two state locus.  Indeed `h(root)=1`
and `k(root)=0`, while homogeneous `Delta(k) != 0` implies `k != 0`.  If such
a `k` were proportional to `h`, evaluation at the root would force the
proportionality scalar, and hence `k`, to vanish.  Thus `h,k` are independent
on `Delta != 0`.

However, two other scalar entries of the same raw operator equations are

```text
R_f[0,1] = -x/128,
R_g[0,1] = 7y/8192.
```

Over `QQ`, full raw CPOBC therefore forces `x=y=0` throughout this sparse
section.  These are the requested low-degree, few-variable additional
relations.

## Rank-optimal linear certificate

After reducing modulo `(x,y)`, each transition is

```text
A_e = F_target F_source^{-1}
```

with all betas one and all alphas zero.  Exact expansion has a particularly
small structure:

```text
raw scalar entries                              3132
nonzero entries                                  783
location of every nonzero entry                 (1,0)
degree of every nonzero entry                        1
root-zero harmonic variables                       62
exact linear rank                                  62
nullity                                             0
```

Streaming exact Gaussian elimination selects 62 original raw rows.  Their
`62 x 62` coefficient minor has

```text
det = 120710493008090868612664506823005578612575380982477722717033332736
    = 2^192 * 3^9 * 977 != 0.
```

The row count is rank-optimal: fewer than 62 linear rows cannot have rank 62
on a 62-variable space.  The frozen coefficient-matrix digest is
`8fd09de12b13fc65ed09a2e2fd7380cfe8a42448f44a16a36d79f3616f5af499`.

Consequently these raw relations force every `z_j=0`, hence `k=0`.

## Localized unit ideal

Work in

```text
QQ[z_1,...,z_62,x,y,tau]
```

and add the exact localizer `tau*Delta-1`.  The unit certificate consists of
two alpha-killing rows, the 62 rank-optimal raw rows, and the localizer:

1. The two top-right rows put `x,y` in the raw ideal.
2. Modulo `(x,y)`, the 62-row nonzero determinant puts every `z_j` in the
   ideal.
3. `Delta=Cf*Cg` is homogeneous of degree four, so it lies in
   `(z_1,...,z_62)`.
4. `tau*Delta-1` then puts `1` in the localized ideal.

Thus the entire `Delta != 0` two-alpha, beta-one raw-CPOBC locus is empty.
This contains and closes the predecessor's nonempty rational section as a
candidate source of full solutions.

## Claim boundary

The certificate fixes the normalized CSG primary coordinate `h`, allows the
full 62-dimensional root-zero harmonic secondary coordinate `k`, fixes all
131 betas to one, and retains only the two displayed alpha coordinates.  It
does **not** classify variable betas, the other 129 alpha coordinates, other
primary harmonic coordinates, the full fixed-CSG `P^61` fibre, or the full
rank-two `Gr(2,63)` open.  It does not decide commutativity.

Supplemental `Q5` is not compiled.  Eq. (113) literal and derived branches
remain separate 25/25 ledgers; Eq. (139) strict and completed domains remain
separate 4/10 ledgers.  None is simultaneously forced or merged.

No solver, Groebner basis, Sage, finite-field scout, or numerical
calculation was run.

Artifact:
`results/v0.4.2_sr2v_variable_harmonic_sparse_section_cpobc.json`

Semantic digest:
`4c8a71689c69d4845344eb44e459b3a5256075188b1f2d71dabc5a7428235efe`

## Reproduction

```powershell
uv run python -m universe_lab.final_theory.sr2v_variable_harmonic_sparse_section_cpobc_v042
uv run pytest -q tests/final_theory/test_sr2v_variable_harmonic_sparse_section_cpobc_v042.py
```
