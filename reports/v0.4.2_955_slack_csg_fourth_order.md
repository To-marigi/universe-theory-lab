# v0.4.2 955 unrestricted slack: CSG四次compatibility・Q escape監査

実行日: 2026-08-10

判定: `V042_955_SLACK_CSG_FOURTH_ORDER_ALL_THIRD_ORDER_JETS_LIFT_Q_ESCAPE_BLOCKED_CERTIFIED`

成果物: [`results/v0.4.2_955_slack_csg_fourth_order.json`](../results/v0.4.2_955_slack_csg_fourth_order.json)

semantic digest: `f59d46a8d52210fe6b63757b8af10484654fbe8ef1483182dcd14c6fcfe817e4`

実装: [`src/universe_lab/final_theory/source_native_955_slack_csg_fourth_order_v042.py`](../src/universe_lab/final_theory/source_native_955_slack_csg_fourth_order_v042.py)

```powershell
uv run python -m universe_lab.final_theory.source_native_955_slack_csg_fourth_order_v042
uv run pytest tests/final_theory/test_v042_955_slack_csg_fourth_order.py -q
```

## 1. 結論

exact `QQ` でfull third-order CSG jet fibreを監査した。4,412 core rowsをpaired
`(Jacobian row, weighted fourth form)` として同時消去したところ、427 independent rowsに
対する3,985 dependent rowsのcompatibility formsは**全て0**だった。従って対角CSG点の
任意のthird-order core jet `(z,a,b)` は四次までliftする。

raw Q fourth formsは6/24 entriesで非零、計2,374項だったが、core Jacobian fourth-jet basisで
消去するとintrinsic formsは24/24全て0。従って全core-compatible fourth lifts上でQ可換子は
`O(t^5)` であり、Q escapeは四次までblockされた。

これは対角CSG点の**形式局所**結論である。unrestricted 955全体、remote components、形式収束、
第五次、Eq113/Eq139を閉じない。

## 2. core fourth-order compatibility

```text
scalar slots                                      4,412
Jacobian rank                                       427
dependent Jacobian rows                           3,985
nonzero compatibility forms                           0
zero compatibility forms                         3,985
compatibility span rank                               0
compatibility basis terms                            0
```

全三次jet fibreは

```text
v = v_particular(z,a) + K b
J_core u + r_core(z,a,b) = 0
```

で表される。compatibility残差が恒等的に0なので、任意の `(z,a,b)` に対して `u` が存在する。
raw compatibility stream digestは
`4ba14a1b2136e8fef7fa8dea6919b6e206d2b4483d63feda4046b36b452ad640`。
空compatibility basis digestは
`4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`。

## 3. Q fourth order modulo core

raw Q streamは24 entries、6非零、2,374項、最大478項。型別は
`z^4=1,240`, `z^2*a=945`, `a^2=63`, `z*b=126`。

```text
first-order Jacobian remainder failures              0
nonzero intrinsic weighted fourth forms               0 / 24
intrinsic span rank                                    0
nonzero compatibility-span remainders                 0 / 24
rank modulo core compatibility span                   0
```

低次消失は固定predecessorで、四次係数の消失は本監査で検査した。従って「Qが四次まで消える」
という結論は妥当である。

## 4. terminal blind directions

既知の16 terminal slack directionsに対し、`z/a/b` weighted indicesを全48本追跡した。
raw core、raw Q、intrinsic Q、Q remainderのmonomial occurrencesは全て0。これらの方向は
coreとQについて四次までsilentである。

## 5. 資源・実行境界

```text
Jacobian basis terms                         135,396
compatibility basis terms                          0
retained-form overhead bytes                1,748,992
conservative actual memory                607,942,656 bytes
add-scaled calls                                28,788
source-term visits                           7,524,868
maximum source terms / call                      1,837
```

全てversioned 8 GiB budget内。raw formsはstream後に破棄し、dense 334,376-column matrixは
作っていない。Gröbner、saturation、有限体、数値、Sage、generic solverは全て0回。

## 6. 次ゲート

四次補正を `u=u_particular(z,a,b)+K c`, `weight(c)=4` とすると、weight 5候補は

```text
z^5     2,869,685
z^3*a   1,020,425
z*a^2      60,025
z^2*b      60,025
a*b         2,401
z*c         2,401
total   4,014,962
```

四次候補の約12倍なので、次は**第五次full fourth-jet-fibre preflightの設計**である。
canonical `u_particular` の疎性標本と別のversioned budgetなしに第五次actualを走らせない。
この認可は形式収束やgeneric solverの認可ではない。

## 7. AI開示

OpenAI Codexが実装・成果物・テスト・本報告を作成した。Lunaはwriterを使わないexact compileを
独立再実行し、保存payloadとの完全一致、全predecessor binding、項数・rank・digest・budget算術・
結論範囲をread-onlyで監査した。Lunaの指摘により427 formの保存overhead 1,748,992 bytesを
actual保守見積りへ追加した。科学的stream digestと結論は変わらない。人間の著者が範囲を選択し
内容に責任を負う。
