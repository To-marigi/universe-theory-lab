# SR2-V reachable-visible search: exact bounded results

Date: 2026-08-01

Current search status: `OPEN`.

No SR2-V terminal verdict is issued by this report.  The exact results below
exclude two declared mechanisms for upgrading the frozen rank-one witness, but
they do not cover arbitrary `GL_2` representations.

## 1. Frozen witness baseline

The independent verifier
`results/v0.4.2_sr2v_baseline_observability.json` has verdict
`SR2V_BASELINE_OBSERVABILITY_AUDIT_CERTIFIED` and semantic digest
`73508f8ad94d97a3147cbd913d687f0b779c740b80a84a4836854053f6f01c62`.
It reconstructs all 407 compiled paths and 87 endpoint states without
importing the v0.4 witness compiler.  The exact classification is

```text
operator noncommutative       true
reachable-span rank over QQ   1
reachable-visible             false
```

All six `Q_1,...,Q_4` commutators are nonzero operators, but each annihilates
all 87 compiled cylinder states.  This is an exact off-reachable-sector
separation, not an observable noncommutative model.

## 2. Transverse reducible rational-torus scout

The first counterexample-first extension uses

```text
A_e = [[a_e,x_e],[0,b_e]],   Omega=e_2,
```

with occurrence identification ON.  The lower diagonal `b_e` is the normalized
`t_j=1` CSG character.  The upper diagonal is sampled from the exact scalar
monomial torus defined by all 783 CPOBC rows, both 25-relation Eq. (113)
branches, and the ten completed Eq. (139) instances.

The scalar exponent system has 132 variables, exact `QQ` rank 83, and an
integer 49-dimensional nullspace.  At each torus point the 1,187 remaining
upper-right equations are linear over `QQ`:

| relation block | rows |
|---|---:|
| raw CPOBC operator equations | 783 |
| Eq. (113), branches kept separate | 50 |
| Eq. (139), completed domain | 10 |
| fixed-vector GC spanning basis | 320 |
| source-matched reachable-state MSR | 24 |

The deterministic campaign checked 180 nonzero rational torus points: basis
directions, CSG-character neighbourhood points, seeded sparse combinations,
signed-base points, and an independent base-three sample.  No candidate left
any of the six `Q` commutators free.  The exact rank census was

| rank | candidates |
|---:|---:|
| 127 | 17 |
| 129 | 2 |
| 130 | 27 |
| 131 | 100 |
| 132 | 34 |

Artifact:
`results/v0.4.2_sr2v_visible_torus_scout.json`.
Verdict:
`SR2V_VISIBLE_TORUS_SCOUT_NO_WITNESS_OPEN`.
Semantic digest:
`7ed0d5f8c3d67528bd6cb2853e8b8887479488453799a43b6645d87437ad028f`.

This is a finite sample, not a cover of the 49-dimensional torus.  Its verdict
is explicitly `NOT_A_SEARCH_TERMINAL_BOUNDED_ANSATZ_ONLY`.

## 3. Exact invariant-line-breaking tangent obstruction

The stronger bounded result concerns every valid upper-triangular base point
with the frozen diagonal characters

```text
A_e(0) = [[p_e,x_e],[0,1]],   Omega=e_1.
```

Allow all four entries of all 131 ON transition matrices, together with the
external `Q_5`, to vary over dual numbers.  This gives 528 first-order
coordinates.  At the frozen noncommutative witness, the complete Jacobian has

```text
rank     455
nullity   73
```

but none of those 73 tangent directions contains a lower-left coordinate of
an actual `n<=4` transition.  The only lower-left kernel coordinate is the
out-of-cutoff `Q_5_EXTERNAL:10` direction.

The reason can be separated from the other 397 tangent coordinates.  At an
upper-triangular base, the lower-left derivative of a product uses only the
two diagonal entries and lower-left derivatives:

```text
d(AB)_21 = dA_21 B_11 + A_22 dB_21.
```

Likewise, for `A=[[p,x],[0,1]]`,

```text
d(A^-1)_21 = -dA_21/p.
```

Neither formula contains the base upper-right value `x`.  Consequently the
lower component of CPOBC, Eq. (113), Eq. (139), fixed-vector GC, and
reachable-state MSR forms one base-`x`-independent 1,187-row system.  Its
exact rank on 132 lower coordinates is 131 and its kernel is precisely

```text
span{Q_5_EXTERNAL}.
```

The normalized row digest is
`dc8f354ac3726532365d48144024fcc1ca53d84957c777f190350d4f45a4ea9c`.
The implementation obtains the same digest from pure-lower differentiation,
full-matrix differentiation at the noncommutative witness, and a zero-upper
control point.  All 407 first-order reachable lower components and all
`6 x 407 x 2 = 4,884` commutator-action components reduce to zero modulo the
Jacobian rows.

Artifact:
`results/v0.4.2_sr2v_baseline_lower_tangent.json`.
Verdict:
`SR2V_BASELINE_LOWER_TANGENT_VISIBILITY_OBSTRUCTED`.
Semantic digest:
`d83afca2c460bad9e8d420e75ad60e1974f3419f0dd655bfc51821d6a02b5a92`.

This proves a first-order obstruction along the declared upper-triangular
rank-one family.  It does not exclude higher-order branching, a disconnected
mixed component, a transverse reducible component outside the sampled torus,
or an irreducible representation.

## 4. Discarded construction and numerical scouts

An endpoint-frame construction

```text
A_(c->d) = p_(c->d) S_d S_c^-1
```

was checked as a design probe.  It makes all 1,529 GC path pairs agree as
operators, permits exact sourcewise harmonic vectors satisfying all 24
reachable-state MSR equations, and readily gives reachable rank two with
visible noncommuting `Q` matrices.  It is not a CPOBC witness: the tested
nonconstant frame choice fails all 783 raw CPOBC equations, both Eq. (113)
branches at three records each, and every completed Eq. (139) instance.  The
construction is therefore rejected, but it isolates CPOBC—not GC or
statewise MSR—as the barrier for this otherwise natural cocycle mechanism.

A mixed nonlinear least-squares scout with commutator and reachable-rank
saturation reduced its residual but produced no exact or rationally
certifiable point.  It is not retained as evidence and does not affect any
verdict.

## 5. Consequence for the next search

Repeating local perturbations of the frozen upper family is now low value.
The next exact search must target one of two genuinely new regions:

1. a disconnected transverse reducible component, preferably after deriving
   an algebraic cover of the 49-dimensional scalar torus; or
2. an irreducible component, detected in dimension two by a nonzero
   determinant of at least one commutator and treated with a small,
   symmetry-reduced principal-open cover.

Any candidate must still pass direct exact substitution into all 783 CPOBC
relations, 712 inverse rewrites, both Eq. (113) branches, both Eq. (139)
domains, all 1,529 fixed-vector GC pairs, all 24 source-matched reachable-MSR
constraints, and every nonsingularity predicate.

## 6. Publication status

The baseline observability audit and the two bounded obstructions strengthen
Paper I's limitations and search narrative.  They do not create a new short
report and do not satisfy an SR2-V terminal.  SR2-V becomes a separate
short-report candidate only after either a reachable-visible exact witness or
a full-profile exact obstruction is certified.

## 7. Reproduction

```powershell
uv run python -m universe_lab.final_theory.weak_d2_observability_v042
uv run python -m universe_lab.final_theory.weak_d2_visible_torus_scout_v042
uv run python -m universe_lab.final_theory.weak_d2_visible_tangent_v042
uv run pytest -q tests/final_theory/test_weak_d2_observability_v042.py
uv run pytest -q tests/final_theory/test_weak_d2_visible_torus_scout_v042.py
uv run pytest -q tests/final_theory/test_weak_d2_visible_tangent_v042.py
```
