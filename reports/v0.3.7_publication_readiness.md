# v0.3.7 publication readiness

Status: **BLOCKED**

The exact research artifacts are locally release-candidate quality: Phase 1
has 21/21 completed QQ charts and 42 matching finite-field scout charts;
Phase 2 has two exact QQ direct-relation certificates; the frozen-artifact
scope correction passes; and the short report has been rendered and visually
checked. Draft `CITATION.cff`, `.zenodo.json`, and JOSS `paper.md` metadata are
present, as are `LICENSE` and `CONTRIBUTING.md`. None of these files means
that an external release, deposit, DOI, or submission exists.

## Resolved by owner decision, 2026-07-30

1. **License selected.** The owner chose the MIT License; `LICENSE` is
   present and recorded in `CITATION.cff` and `.zenodo.json`. MIT was taken
   over Apache-2.0 because this is a single-author research artifact with no
   patent surface, and because the shorter text can be reproduced verbatim
   without transcription risk. JOSS and Zenodo treat the two equivalently.
2. **Community guidelines added.** `CONTRIBUTING.md` covers issue reporting,
   support expectations, the frozen-artifact and exact-over-numeric
   invariants, and mandatory disclosure of AI-assisted contributions.

## Blocking items

1. **CI workflow added but not yet observed green.**
   `.github/workflows/ci.yml` runs the portable path on `ubuntu-latest`
   under Python 3.12: `uv sync --frozen`, `uv run ruff check .`,
   `uv run pytest -ra`. Those three commands were confirmed locally on
   2026-07-30 (`ruff` clean, 331 passed). The mypy job is deliberately
   advisory: `uv run mypy` reports 11 pre-existing errors, all in
   `src/universe_lab/final_theory/d2_sage_backend_v035.py`, which is
   proof-bearing solver code bound to frozen certificate digests and is
   therefore not edited during release preparation. The workflow has never
   executed on GitHub, so no green run exists yet. Docker/Sage exact reruns
   remain outside CI by design.
2. **Software-maturity review remains human-owned.** Release history,
   support expectations, and fit with JOSS scope must be assessed by the
   owner before submission. Passing certificates do not determine editorial
   eligibility.

## Actions deliberately not performed

- no GitHub release;
- no Zenodo deposit or DOI reservation;
- no JOSS submission;
- no claim that `FINAL_THEORY_OPEN` has changed.

The machine-readable companion is
`results/v0.3.7_publication_readiness.json`.
