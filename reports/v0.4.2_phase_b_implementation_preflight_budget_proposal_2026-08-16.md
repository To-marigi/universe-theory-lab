# Phase-B implementation-preflight budget proposal — 2026-08-16

Status: **`PROPOSAL_ONLY_OWNER_APPROVAL_REQUIRED`**

This packet instantiates a bounded budget for the next implementation
preflight.  It is deliberately not an authorization: `owner_approval_present`
is `false`, new trajectories are capped at zero, and solver calls are capped at
zero.

## Proposed scope

The proposal covers deterministic fixtures, exact n<=5 equivalence and
relabeling checks, capped naturally labeled down-set comparisons, portable RNG
replay, bounded observable equivalence, and hard resource-supervisor fixtures.
It does not open candidate or control production at N=8...60.

| limit | proposed value |
|---|---:|
| fixture count | 10 |
| maximum fixture n | 13 |
| down-sets per relation | 4,096 |
| total wall time | 120 s |
| worker wall time | 30 s |
| memory | 1,024 MiB |
| scratch disk | 128 MiB |
| retries | 0 |
| new trajectories | 0 |
| solver calls | 0 |

The values are bounded implementation-test limits, not estimates for the
production campaign.  The proposal is based on the existing cost preflight
(`0.9513146999524906` s and `892712` traced bytes) and hard-supervisor fixture
certificate; those observations contain no trajectories and no solver calls.

## Required owner decisions

The owner must approve or revise the limits, confirm that the scope is fixture
only, and provide a separate versioned production measurement budget before any
trajectory is generated.  The spectral window, seed counts, simultaneous
interval method, and negative-control equivalence rule must be frozen from
controls before candidate access.

## Boundary

Until approval is recorded, this proposal must not be consumed by an execution
command. The global verdict remains `FINAL_THEORY_OPEN`; no continuum,
dimension, Spin-2, or universal-coupling claim is added.
