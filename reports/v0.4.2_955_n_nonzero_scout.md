# v0.4.2 955 source-native `N!=0` scout

## 判定

`V042_955_N_NONZERO_SCOUT_NO_WITNESS_OPEN`

これは一般可換性の証明ではなく、明示した exact 上三角族における負の scout である。

## 再現可能な exact scout

各 ON quotient orbit に一変数を置き、全 transition を

\[
A_e=\begin{pmatrix}p_e&x_{[e]}\\0&1\end{pmatrix},
\qquad \Omega=e_1,
\]

とする。`p_e` は `t_j=1` の有理 CSG character である。全165 occurrence の行列式は
`p_e!=0`。source `p1-0` では MSR operator residual の右下成分が恒等的に1なので、
この族全体が strong-MSR slice の外、すなわち `N!=0` にある。一方、第一列残差は
24 source 全てで零なので reachable-state MSR を満たす。

783 raw CPOBC 式の exact QQ rank は108。320 strong-GC spanning-basis 式を加えた
rank は114、131変数に対する nullity は17である。六つの `Q_1,...,Q_4`
commutator functional を各々追加しても rank は114のままであり、この族では全て零に
強制される。したがって非可換 witness は存在しない。

再現:

```powershell
uv run pytest -q tests/final_theory/test_source_native_955_n_nonzero_scout_v042.py
```

期待値は `3 passed`。machine result の semantic digest は
`27935a8b7908f26179036b7732c5999f5ffe3b4b23363768ab714a83f90853d3`、
file SHA-256 は
`037a0d512233dd6fc9470f660860506bc696a03fd335e6592c8850226e10db00`。

## 非証明の exploratory scratch

次の結果は今後の再実装候補であり、machine certificate には含めない。

- endpoint-potential の upper-unipotent axis は exact rank 69/nullity 17 で Q 可換。
- lower-unipotent axis は reachable-MSR を加えると exact rank 86/86 で潰れる。
- 258変数の mixed local-`SL_2` cell を三seed・各150評価した数値試行では、目標
  commutator を約0.0027まで動かせたが、全783 CPOBC residual の最大値が
  0.097--0.099残った。全候補は無効であり、証拠には使わない。

次に探索すべき対象は、structured source inventory が宣言した48個の `N!=0`
principal-open patch のうち、混合 upper/lower deformation を含む exact
scalar-polynomial manifest である。Sage、Gröbner basis、saturation は今回未実行。
