# SR2-V additive MSR on the primitive scalar Laurent torus

Date: 2026-08-02

Verdict:
`SR2V_ADDITIVE_MSR_LAURENT_SMOOTH_CSG_POINT_CERTIFIED_NONTERMINAL`

## Exact result

The primitive scalar-lattice certificate gives a split torus `G_m^29` for the
observed diagonal character before additive MSR. Substitution of its integral
kernel map into the 24 source-matched MSR equations gives 24 explicit Laurent
polynomials over `QQ`, with source-stage census

```text
stage 1 / 2 / 3 / 4: 1 / 2 / 5 / 16.
```

Every Laurent equation is also stored after multiplication by an explicit
monomial, so a later ordinary-polynomial computation can preserve the torus
boundary without silently discarding negative exponents.

The normalized CSG character has an integral lift to the 29 torus coordinates:

```text
(-4,-1,-2,-1,-3,-1,-2,-2,-2,-2,-3,-4,-2,-1,-1,
 -2,-2,-3,-3,-3,-3,-4,-2,-2,-1,-3,-4,-2,-5),
```

with base 2. Direct exact evaluation gives zero for all 24 equations.

## Smooth local geometry

At this rational point, the logarithmic Jacobian

```text
(t_j d/dt_j) F_i
```

has shape `24 by 29` and rank 24 over `QQ`. All 24 source equations are
independent. The selected first-24-coordinate minor is

```text
-5 / 35184372088832 = -5 / 2^45,
```

so the additive-MSR locus is smooth of codimension 24 and local dimension 5
inside `G_m^29` at the normalized CSG point.

One tangent direction and one exact global torus factor are solely the
cutoff-external `Q5`. The additive equations contain no `Q5` coordinate.
Consequently the actual-edge scalar locus has tangent dimension four at this
point, while the fifth dimension is the independent `G_m(Q5)` factor.

This supplies an exact local target for the subsequent upper-right cocycle
analysis; it replaces finite sampling near the normalized scalar character.

## Scope boundary

This is not a global classification of the 24-equation Laurent variety. It
does not prove that every scalar solution is a CSG character, solve any
upper-right affine-cocycle fibre, test a commutator principal open, or prove a
witness or obstruction for the full weak/weak profile. No Sage, Groebner,
finite-field, numerical, or other solver was invoked. SR2-V remains open.

Machine-readable artifact:
`results/v0.4.2_sr2v_additive_msr_laurent.json`.

Semantic digest:
`53241bd2973c41153d145d6e2fee2f082c4631f7aeba76e30f0e89cca7df3b8b`.

## Reproduction

```powershell
uv run python -m universe_lab.final_theory.sr2v_additive_msr_laurent_v042
uv run pytest -q tests/final_theory/test_sr2v_additive_msr_laurent_v042.py
```
