# v0.4.2 955 unrestricted slack: CSG点の三次形式監査

実行日: 2026-08-10

判定: `V042_955_SLACK_CSG_THIRD_ORDER_ALL_SECOND_ORDER_JETS_LIFT_Q_ESCAPE_BLOCKED_CERTIFIED`

成果物: [`results/v0.4.2_955_slack_csg_third_order.json`](../results/v0.4.2_955_slack_csg_third_order.json)

semantic digest: `53d7818f4bff6e5d3c556cdb137644feeae73a4bde50f74d5cb84feb8db16bfa`

実装: `src/universe_lab/final_theory/source_native_955_slack_csg_third_order_v042.py`

```powershell
uv run python -m universe_lab.final_theory.source_native_955_slack_csg_third_order_v042
uv run pytest tests/final_theory/test_v042_955_slack_csg_third_order.py -q
```

## 1. 結論

対角CSG点のfull second-order jet fibre

```text
x(t)=x_CSG+tKz+t^2(w_particular(z)+Ka)+t^3v
```

上で、4,412 core scalar equationsをexact `QQ` で三次まで監査した。427 Jacobian pivotsへ
同時消去した後に残る3,985本の三次compatibility formsは**全て恒等的に0**である。
従って、任意の二次core jet `(z,a)` に対して少なくとも一つの三次係数 `v` が存在し、
coreを `O(t^4)` まで満たす。

Q可換子はraw third formsでは6/24 entriesが非零・合計458項だった。しかし同じcore
Jacobian-jet basisへreduceすると、24 entries全てのintrinsic weighted third formが0。
従って任意のcore-compatible third jet上で全Q可換子は三次まで消える。一次・二次に続き、
三次にも局所Q escapeはない。

これは**指定CSG点の局所・形式的・三次まで**の結果である。四次以上、formal convergence、
remote component、Eq113 / Eq139、unrestricted source-native 955のterminal obstructionは
証明していない。full 955は `OPEN` のままである。

## 2. 入力とfull jet fibre

slack inventory、mixed manifest、一次tangent、二次監査、三次preflight、8 GiB budgetを
raw SHA-256 / semantic digestで固定した。基点では全131 matrices `diag(p_e,1)`、全131
determinants nonzeroを再確認した。

二次監査の427 pivot jetsからcanonical `w_particular(z)` を再構成し、digest
`a9b48e13cdee32a524fe4fb0a53c1fae464d3b48bfffd8221b28b55f57e769cc`
がpreflightと一致。49 `z` variablesと49 `a` variablesにより全ての二次core jetsを覆う。

## 3. 三次core compatibility

各core equationの三次係数は

```text
J_core*v + r_core(z,a) = 0
```

である。`r_core` は `z^3` と `z*a` の23,226候補monomialからなる疎形式。raw streamは
preflightと完全一致した。

```text
scalar slots                         4,412
Jacobian rank                          427
dependent Jacobian rows              3,985
raw nonzero compatibility forms          0
raw zero compatibility forms         3,985
compatibility span rank                   0
compatibility basis terms                 0
```

raw compatibility stream digestは
`4ba14a1b2136e8fef7fa8dea6919b6e206d2b4483d63feda4046b36b452ad640`。
全formが空なので、同じidentifier列を持つ二次監査のzero stream digestと一致する。

Jacobian third-jet basisは31,761 terms、digest
`d8586c8cc45d57bfa5d4c5d7ae71146378606762e5212f5b611f5df03c14d287`。
3,985 dependent reductionsを含むJacobian add-scaledは28,788 calls、1,772,515 source-term
visits。compatibility basis reductionは必要なく0回だった。

## 4. Q可換子の三次項

6可換子・24 scalar entriesのraw三次項は次の6 entriesだけ非零だった。

```text
[Q1,Q2]01 87 terms    [Q1,Q3]01 87 terms
[Q1,Q4]01 72 terms    [Q2,Q3]01 71 terms
[Q2,Q4]01 70 terms    [Q3,Q4]01 71 terms
```

合計458 terms。しかし各pair `(dQ, r_Q)` をcore Jacobian third-jet basisへ同時reduceすると、
raw係数stream digest
`654d27aa3211717c9de2a4692ed35f9274ed89aa5d24a40adf981229927a58a6`
はpreflightと一致し、その上で

```text
first-order Jacobian remainders          0 / 24
nonzero intrinsic third forms            0 / 24
intrinsic span rank                       0
nonzero compatibility-span remainders    0 / 24
rank modulo core compatibility span       0
```

となった。raw非零をescapeと誤読してはならない。core equationの三次補正を用いると全て
exact cancellationする。

## 5. terminal slack blind directions

既知の16 terminal slack coordinatesに対応する `z` / `a` indicesは、raw core third forms、
raw Q third forms、intrinsic Q forms、Q remaindersのいずれにもmonomial occurrence 0。
従ってcoreとQに対して三次までsilentである。これはexact自由族や全次数silenceの証明ではない。

## 6. 資源と実行境界

compatibility basisは空だったため、実際の保守basis memory estimateは553,132,544 bytesで
8 GiBを大きく下回った。raw formsは1本ずつstreamして破棄し、dense 23,226-column matrixは
作っていない。

- exact sparse `QQ` third-order compatibility audit: 1
- dependent compatibility reductions: 3,985
- Q intrinsic reductions: 24
- Gröbner / saturation: 0
- finite-field: 0
- numerical: 0
- Sage: 0
- generic solver: 0

## 7. 次ゲート

全三次jetがliftしQも三次までblockされたため、次は四次のfull third-order jet fibreを
設計する。三次補正を

```text
v = v_particular(z,a) + K b,  weight(b)=3
```

とすると、weight 4の候補monomialは

```text
z^4      C(52,4)       = 270,725
z^2*a    C(50,2)*49    =  60,025
a^2      C(50,2)       =   1,225
z*b      49^2          =   2,401
total                       334,376
```

まで増える。三次の14倍超なので、直ちに四次compatibility reductionへ進まず、まずcanonical
`v_particular` の項数、raw weight-4 jets、Jacobian basis growth、保守memoryをversioned
preflightする。8 GiB内のfail-closed budgetが立つ場合だけ四次監査へ進む。

## 8. AI 開示

OpenAI Codexが三次exact監査モジュール、成果物、テスト、本報告、状態・引き継ぎ更新を
作成した。Lunaは数式範囲・数値・digest・budget・過剰主張をread-onlyで独立監査した。
人間の著者が範囲を選択し内容に責任を負う。
