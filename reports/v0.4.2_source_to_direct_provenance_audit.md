# v0.4.2 source-to-direct provenance audit

The triangular scout selected `msr:p1-0`, `msr:p2-0`, and `msr:p2-2` as
strong-MSR **source-stage constraint IDs**.  They are not IDs in the frozen
literal Q5-free direct-operator relation list used by the 21-chart QQ worker.
That list has 21 strong-MSR relations, all at source stages 3 and 4; the three
candidates are at stages 1 and 2.

Therefore a claim that the existing worker can solve “CPOBC 700 plus one of
these three IDs = 701 direct relations” would have no accepted provenance.
No solver run is scheduled.  This is reported as
`V042_SOURCE_TO_DIRECT_PROVENANCE_REQUIRED`, not as a negative mathematical
result.

This was the initial fail-closed result. The follow-up exact lift audit in
`v0.4.2_source_to_q_lift_design.md` supersedes the “mapping absent” diagnosis:
all three source constraints have unique zero Q residuals. Consequently there
is no nonzero direct relation for the worker to select, and the proposed
701-relation campaign is invalid as stated.

Reproduce the machine-readable audit with:

```powershell
uv run python scripts/reproduce_v042_single_strong_msr.py --write-audit
```

The current semantic digest is
`8718c6d9266486ec6df775bdd8027b5e64658451a0b3fd09b35498bb5faf5237`.
