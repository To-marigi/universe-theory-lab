# SR2-V corrected CSG-compatible fixed-(h,k) obstruction

Date: 2026-08-02
Verdict: **`SR2V_CORRECTED_CSG_FIXED_HK_NONSINGULAR_CPOBC_UNIT_IDEAL_OBSTRUCTION_CERTIFIED_NO_SOLVER_RUN`**

## Corrected chart

The former fixed uniform-terminal harmonic state assignment is not compatible
with raw CPOBC.  Its full 262-parameter chart contains the constant generator
`2/117`, so it is empty before nonsingularity is imposed.

The corrected construction starts from the normalized finite-CSG character

```text
p_e = 2^(precursor_width-maximal_precursor_count) / 2^stage
      (t_j=1).
```

The 131 actual ON edge values were reconstructed over `QQ`; all are nonzero.
For every one of the 407 labelled paths,

```text
h_endpoint = product(p_e along the path)
```

is path-independent on all 87 endpoints.  Exact checks give

```text
h_root = 1,
h_target = p_e*h_source                       on all 131 edges,
h_source = sum multiplicity*h_target          at all 24 sources.
```

An independent harmonic coordinate `k` is obtained from the lexicographically
first two terminal boundary coordinates, weighted by their opposite root
weights and extended backward.  It has `k_root=0`.  The support pair
`p5-0000000,p5-0000002` has determinant `-11/1024`, proving reachable rank two.

For `v_c=(h_c,k_c)^T` and

```text
F_c = [[h_c,0],[k_c,1]],
```

the complete matrix fibre satisfying `A_e v_source=v_target` is

```text
A_e = F_target * [[1,alpha_e],[0,beta_e]] * F_source^-1.
```

Completeness follows because `F_target^-1 A_e F_source` must have first column
`(1,0)^T`, leaving the unique free second column `(alpha_e,beta_e)^T`.
Furthermore,

```text
det(A_e) = p_e*beta_e,
```

so an actual transition is nonsingular exactly when `beta_e!=0`.
All 407 fixed-vector path states, all 1,529 same-endpoint GC pairs, and all 24
reachable-state MSR constraints vanish identically on the chart.

## Raw CPOBC census

Custom sparse `QQ` arithmetic expands all 783 raw CPOBC equations into the 262
variables `alpha_e,beta_e`:

```text
operator equations                              783
scalar entries                                 3132
zero / nonzero entries                       442 / 2690
total terms                                   12860
maximum terms in one entry                       23
maximum total degree                              3
entries with nonzero constant coefficient        180
pure nonzero constant entries                       0
```

The previous uniform-terminal generator
`cpobc-relation-0ca7a22bed9cdf4bc14a`, Eq. (103), entry `(0,0)` becomes the
zero polynomial exactly.  Thus the old one-row constant obstruction is
genuinely repaired; it is not merely hidden by a change of notation.

There are 327 nonzero raw scalar rows containing no `alpha` variable.  All are
binomials, with 654 total terms and maximum-degree census

```text
degree 1     22
degree 2    303
degree 3      2
```

Their `327 x 131` beta exponent-difference matrix has exact rank 89, nullity
42, and nonzero Smith factors `1^89`.  The corresponding frozen digests are:

```text
variable order      34ce26e270a7e58ed71219aa16be544769704b5bf535c48aa7050b59c5dc205b
exponent matrix     29ccc8f815d01c0c159b862cdacb531fb4a88a01a3315e33b549f179961a74a4
audited rows        7e860a6c3d445bdd734c019a65bd2b72a2b8ddc84a367f86e9c00a6b2a888ae4
```

## Nonsingular two-row unit obstruction

The corrected chart nevertheless has a stronger obstruction on the selected
fixed `(h,k)` slice.  Two independently reconstructed raw Eq. (103) entries
contain no `alpha` variables:

```text
f = beta_u + 1/8,
relation  cpobc-relation-ecdf5451a0bcf06c8da5,
entry     (1,0),
u         cpobc-transition-orbit-92dc21cb80d8c56a73bf,
p_u       1/8;

g = 8*beta_v*(1-8*beta_u),
relation  cpobc-relation-8c0a09869cf24abeac93,
entry     (1,0),
v         cpobc-transition-orbit-983362874bc6f5900920,
p_v       1/16.
```

At orbit `v`, `det(A_v)=(1/16)beta_v`.  Nonsingularity therefore requires
`beta_v!=0`.  Introduce its exact inverse coordinate with

```text
l = rho*beta_v - 1.
```

Then the following identity is checked by direct sparse-polynomial arithmetic:

```text
1 = (rho/16)*g + 4*rho*beta_v*f - l.
```

Consequently the localized raw-CPOBC ideal is the unit ideal.  The
nonsingular relation variety is empty for this selected corrected-CSG fixed
`(h,k)` state assignment.  No Gröbner basis or solver is needed.

## Beta=1 diagnostic

The corrected `beta_e=1` shear specialization still has 151 pure nonzero
constant entries.  Its first is

```text
cpobc-relation-0744145e3fa7b1f240ee, Eq. (103), entry (1,0) = 15/2.
```

It is therefore empty independently of `D12`.  The old D12 shear should not
be reused or rerun on the corrected states.

## Scope and unresolved ledgers

This result covers only the selected corrected-CSG fixed `(h,k)` nonsingular
slice.  It does not cover other choices of the independent harmonic `k`, other
rank-two state assignments, or a global chart cover.  The unrestricted
singular locus remains unresolved.

Supplemental `Q5` remains a separate generic four-coordinate matrix.  The two
Eq. (113) branches remain separate `25/25` unresolved ledgers, and the Eq.
(139) printed/completed domains remain separate `4/10` unresolved ledgers.
The unit identity already lies in raw CPOBC plus the required beta localizer,
so adding any of these ledgers cannot restore this fixed slice; their own
equations have nevertheless not been compiled or merged here. This is not an
SR2-V terminal.

No solver, Sage, Gröbner basis, finite-field calculation, numerical scout, or
campaign budget was used.

Artifact:
`results/v0.4.2_sr2v_corrected_csg_fixed_hk_obstruction.json`

Semantic digest:
`2b6c8483598cf73bbf9dd2365dd2c7828c10bc0aedb72c58a84c47142c215b7f`

## Reproduction

```powershell
uv run python -m universe_lab.final_theory.weak_d2_state_native_corrected_csg_rank2_v042
uv run pytest -q tests/final_theory/test_weak_d2_state_native_corrected_csg_rank2_v042.py
```
