# SR2-V Eq. (120) repair-pair rejection scout

Date: 2026-08-02

Verdict:
`SR2V_TRANSVERSE_EQ120_THREE_PAIR_MINOR_SUBCOVER_EXACTLY_REJECTED_FULL_M0_SURVIVES_NONTERMINAL`

Semantic digest:
`dea028466f41a6a186fbcb09f7b5ba8a74d12834dc7f348d0a1f5f04abf4bcf7`

This exact scout follows the global 127-by-127 non-`Q` unit minor and the
four-chart Eq. (120) base-cover ablation.  It first records exact rejections of
two finite-ledger repair selections.  A third row pair repairs the Eq. (120)
kernel at both older points, but a new exact point makes all three proposed
pair minors vanish simultaneously while remaining inside both `D(g2)` and
`D(g3)`.  Thus these three pair-minor opens do not cover either chart.  The
complete common core still contains every commutator at all three points.

## Frozen inputs and coordinates

The artifact binds, by raw and semantic SHA-256, the frozen results

```text
v0.4.2_sr2v_transverse_common_core_unit_minor.json
v0.4.2_sr2v_transverse_base_cover_ablation.json
```

The first supplies the rank-127 pivot block.  Schur rows use columns
`(Q1,Q2,Q3,Q4,Q5)`.  On the Eq. (120) kernel a row `rho` restricts to

```text
A*h + B*w + C*q5,
A=rho.delta,  B=rho.(b/b1),  C=rho_Q5.
```

For two `Q5`-free rows, the exact pair-minor identity uses

```text
D=A_r*B_s-A_s*B_r.
```

When `D` is nonzero at a point, the two restrictions span the target
`e_w=(0,1,0)` there.

## Two older finite-selection rejections

On the `g2` slice point

```text
(u12,u13,u15,u44,u45)=(1,2,1,3,3),  g2=-1/48,
```

the Eq. (120) base sources `{29,147}` plus proposed patch `{32,120}` have
Schur ranks `(candidate,star,augmented)=3/1/4`.  On the `g3` slice point

```text
(u12,u13,u15,u44,u45)=(1,3,1,2,2),  g3=1/256,
```

base sources `{29,548}` plus patch `{424,544}` also have ranks `3/1/4`.
These are exact failures of the small selected sets, not of the complete
common core.

The `Q5`-free pair `{8,14}` repairs the Eq. (120) kernel pointwise at both
older points:

```text
D_8,14 = 975/2048  at the g2 failure point,
D_8,14 = 225/4096  at the g3 failure point.
```

Its restricted ranks `(pair,e_w,augmented)` are `2/1/2` in both cases.  This
is a pointwise certificate, not a global localization claim.

## Exact common zero of all three proposed pair minors

Now take the exact positive rational point

```text
(u12,u13,u15,u44,u45)=(1,1/2,1,2,2).
```

The chart values are

```text
(g2,g3,g4)=(1/96,1/256,0).
```

Hence the point lies in `D(g2) intersect D(g3)` (and on `V(g4)`).  The star
rows `c12,c13,c14` are

```text
(1/24, 0, 0, 0, 0)
(1/64, 0, 0, 0, 0)
(0,    0, 0, 0, 0).
```

The three candidate pairs reduce to

```text
{32,120}:
(0,      0,       0,      0,     0)
(5/1312, 15/164, -15/82, -5/16, 0)

{424,544}:
(25/1312, -45/1312, 45/164, -15/16, 0)
(0,         0,        0,       0,    0)

{8,14}:
(0,    0,    0, 0, 0)
(-1/8, 9/16, 0, 0, 0).
```

Their Eq. (120)-kernel `(A,B,C)` restrictions are respectively

```text
{32,120}:  (0,0,0), (-5/5248,-5/1312,0)
{424,544}: (-15/5248,-5/1312,0), (0,0,0)
{8,14}:    (0,0,0), (-3/128,1/16,0).
```

Thus every pair has `D=0`.  For each, both the ambient Schur ranks
`(candidate,star,augmented)` and the restricted ranks `(pair,e_w,augmented)`
are `1/1/2`.  All three pair-minor opens miss this point even though two Eq.
(120) chart functions are nonzero.

## Full-module control and consequence

At each of the three exact points,

```text
rank(M0)=rank(M0 + all six commutators)=131.
```

The new point therefore rejects only the proposed subcover

```text
D(D_32,120) union D(D_424,544) union D(D_8,14).
```

It does not reject `D(g2)`, `D(g3)`, `D(g4)`, the full common core, or
commutativity.  A valid continuation must find additional repair minors and
prove a unit-ideal/localized cover, derive a direct localized full-module row
identity, or find a genuine full-`M0` rank jump.  Another finite point ledger
alone is not a certificate.

## Reproduction

```powershell
uv run python -m universe_lab.final_theory.sr2v_transverse_eq120_repair_pair_scout_v042
uv run pytest -q tests/final_theory/test_sr2v_transverse_eq120_repair_pair_scout_v042.py
```

Expected focused result: `9 passed`.

## Claim boundary

All displayed values and ranks are exact over `QQ`.  The two older points
reject only their named finite row selections; the third point rejects only
the three named pair-minor opens as a subcover.  The targeting factors used to
find the older points remain search rationales, not global determinant
factorization certificates.  This artifact is nonterminal and issues no
SR2-V commutativity or noncommutativity verdict.
