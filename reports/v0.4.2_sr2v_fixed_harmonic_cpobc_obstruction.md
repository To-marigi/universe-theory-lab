# SR2-V fixed harmonic state assignment: exact CPOBC obstruction

Date: 2026-08-02
Verdict: **`SR2V_FIXED_HARMONIC_FULL_ALPHA_BETA_CPOBC_UNIT_IDEAL_OBSTRUCTION_CERTIFIED_NO_SOLVER_RUN`**

## Main result

The state-native chart attached to the currently frozen harmonic rank-two
state assignment has no raw-CPOBC point, even before imposing nonsingularity,
`D12`, or supplemental `Q5` conditions.

This was verified independently from the raw relation and occurrence-reduction
ledgers.  For each of the 131 ON edge orbits the full affine family was rebuilt
over `QQ` as

```text
A_e = F_target * [[1, alpha_e], [0, beta_e]] * F_source^-1.
```

Thus all 131 `alpha_e` and all 131 `beta_e` remain free.  On this full
262-parameter chart, raw relation
`cpobc-relation-0ca7a22bed9cdf4bc14a`, Eq. (103), has

```text
(A_n*A'_m - A_m*A'_n)[0,0]
  = 5/117 - 1/39
  = 2/117.
```

It is a constant polynomial with no `alpha` or `beta` support.  Therefore, in

```text
R = QQ[alpha_e,beta_e : 131 actual ON edge orbits],
```

the raw CPOBC ideal contains

```text
(117/2)*(2/117) = 1.
```

The ideal is the unit ideal and its relation variety is empty for this fixed
state assignment.  Localizing at every `beta_e`, at `D12`, or at `det(Q5)`
cannot restore a point.

## Independent reconstruction and census

The certificate does not import the D12 compiler or its polynomial helpers.
It reconstructs from the two frozen raw ledgers:

```text
transition occurrences                  165
ON edge orbits                          131
endpoint states                          87
nonterminal MSR sources                  24
free alpha/beta coordinates             262
raw CPOBC operator equations             783
raw scalar entries                     3132
pure nonzero constant entries             95
relations carrying such an entry          95
```

All 95 full-chart constants occur in entry `(0,0)`.  Their exact record digest
is
`0b32fd734dc5b6bc84efdb16c4905cce72ec8669f9640bcc2ca3b064792ced13`.

The harmonic recurrences and rank-two support determinant were recomputed over
`QQ`.  Each transition determinant is a nonzero rational multiple of its
corresponding `beta_e`; hence the usual nonsingular domain is precisely the
appropriate beta localization.  Since the unsaturated ideal already contains
one, that localization remains empty.

## Beta=1 D12-manifest cross-check

Specializing every `beta_e=1` independently reproduces all **178** pure
nonzero constant entries stored in the D12 shear manifest:

```text
entry (0,0)       95
entry (1,0)       83
total            178
relations        171
```

The complete independent and manifest record lists have the same digest,
`df55b36d50ae69dfdac6767bbc84f2613252ea132c3de8d7b1a62638b5128e4a`.
In particular,
`cpobc-relation-1decb1e77431d5355ada`, Eq. (103), satisfies

```text
(A_n*A'_m - A_m*A'_n)[1,0] = 7/663,
(663/7)*(7/663) = 1.
```

This cross-check uses the D12 manifest only for comparison; the graph, states,
matrices, residuals, and unit-ideal proof do not use it.

## Claim boundary

The result closes the complete 262-parameter state-native chart for **one
fixed harmonic rank-two state assignment**.  It does not cover:

- other rank-two state assignments;
- all possible rank-two state assignments;
- the complete fixed-vector-GC/reachable-state-MSR profile;
- other SR2-V branches or SR2-V as a whole;
- a global commutativity or noncommutativity theorem.

No solver, Gröbner basis, Sage, finite-field calculation, numerical scout, or
campaign budget was used.  The prior D12 solver campaign should not be run on
this now exactly obstructed fixed state assignment.

Artifact:
`results/v0.4.2_sr2v_fixed_harmonic_cpobc_obstruction.json`

Semantic digest:
`d5514f31bf6caef5ca102ef8ce78f0bcf137e72922c0f230c716386f22cffc7d`

## Reproduction

```powershell
uv run python -m universe_lab.final_theory.weak_d2_state_native_fixed_harmonic_cpobc_obstruction_v042
uv run pytest -q tests/final_theory/test_weak_d2_state_native_fixed_harmonic_cpobc_obstruction_v042.py
```
