# v0.4.2: mixed source-native 955 polynomial manifest

Status: **`V042_955_MIXED_SOURCE_NATIVE_SCALAR_MANIFEST_READY_NO_SOLVER_RUN`**

This artifact expands the exact mixed ansatz

```text
A_e = [[p_e, x_e], [y_e, 1]],
p_e = CSG(t_j=1),  Omega=e1,
```

over all 131 ON quotient orbits.  It retains source occurrences, not an
Eq. (108) or Eq. (112) reconstruction.  The result contains sparse QQ
polynomials for all 783 raw CPOBC matrix residuals, all 320 strong-GC basis
matrix residuals, 24 MSR operator residuals, and the 24 reachable-state vector
residuals formed from canonical strong-GC paths.

There are 262 scalar ansatz variables (`x_e,y_e`).  Every one of the 165 raw
nonsingularity conditions is recorded as `det(A_e)=p_e-x_e*y_e`, with 131
distinct ON quotient factors.

At source `p1-0`, the lower-right entry of the MSR **operator** residual is
the constant polynomial `1`.  Thus every point of this ansatz lies outside the
strong-MSR `N=0` slice, independently of `x` and `y`.  This is a structural
certificate, not a claim that the reachable-MSR equations have been solved.

The source-native Eq. (120) certificate is bound as a global nonsingular-CPOBC
prerequisite for the ratio pairs `(R2,R3)`, `(R2,R4)`, and `(R3,R4)`.  The
manifest exposes only future routing labels `S1/S2/S3`; it does **not** reuse
the old 21 chart ideals or assert a chart cover.

Eq. (113) derived/literal branches (25/25) and Eq. (139) printed/completed
domains (4/10) are retained as separate direct witness-validation gates.  They
are not mixed into the source coordinate equations.

No solver or Sage process is started.  The output is a structured sparse
scalar-polynomial manifest only: it neither supplies a witness nor proves
commutativity.

The checked-in artifact has semantic digest
`13dc6cc8ec89395ba86bc66c5d96e25f79ed8cc5bbf9c6cc2b6925d1256430f2`.
Its four source blocks contain 23,166 stored polynomial terms in total.

## Reproduction

```powershell
uv run python -m universe_lab.final_theory.source_native_955_mixed_manifest_v042
uv run pytest -q tests/final_theory/test_source_native_955_mixed_manifest_v042.py
uv run ruff check src/universe_lab/final_theory/source_native_955_mixed_manifest_v042.py tests/final_theory/test_source_native_955_mixed_manifest_v042.py
uv run mypy src/universe_lab/final_theory/source_native_955_mixed_manifest_v042.py tests/final_theory/test_source_native_955_mixed_manifest_v042.py
```
