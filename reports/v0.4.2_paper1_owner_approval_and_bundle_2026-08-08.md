# Paper I v0.4.2 owner approval and rebound bundle — 2026-08-08

## Scope

This record captures the owner's approval of a final source commit and the
resulting rebuild of the Zenodo bundle. It does not authorize deposit,
publication, DOI reservation, or manuscript freeze. It supersedes only the
`Packaged commit` table in
`reports/v0.4.2_paper1_final_audit_2026-08-07.md` and the "Verified production
bundle" table in `reports/v0.4.2_paper1_zenodo_handoff_2026-08-07.md`, both of
which were bound to the prior commit `12511276a142a431b46a2fbb11413d85169f982d`.

## Owner approval, recorded verbatim in substance

The owner approved `1877d6c4fe2fc824a969176ab851807b7a6603d8` as the final
source commit for Paper I v0.4.2, on the stated basis that:

- the production bundle built successfully against that commit;
- the strict verifier passed with every error category empty;
- 38 tests passed, all three validators reported `OK`, and `ruff` was clean;
- the PDF hash matches `owner_decision.json`.

The owner explicitly instructed that the prior bundle, built against
`12511276a142a431b46a2fbb11413d85169f982d`, must not be reused; the bundle used
for deposit must be regenerated against `1877d6c...`. The owner also stated
explicitly that this approval is a source-commit approval, not a deposit
approval, and that findings F2 (fresh predraft literature gate) and F4 (HAL
bundle) remain open.

This is a scope approval of the commit, not a rebinding action. No repository
file was changed as a result of the approval itself; the rebuild below uses the
already-committed state at `1877d6c...`.

## Rebuilt and re-verified bundle

The bundle was rebuilt against `1877d6c4fe2fc824a969176ab851807b7a6603d8` twice
in this session, once into a scratch path and once into the durable path the
project's own upload checklist names
(`UPLOAD_CHECKLIST.md` step 1, `..\paper1-v0.4.2-upload`, outside the
repository). Both builds produced byte-identical output, which is expected: the
supplement archive is deterministic and both were built from the same commit.
The durable copy is the one recorded here.

| Field | Value |
| --- | --- |
| Packaged commit | `1877d6c4fe2fc824a969176ab851807b7a6603d8` |
| PDF | 428286 bytes, `c112b987b4efb76312892481dd033598b939a0a8becfc4ac0e0eca4070dbfa2e` |
| Supplement | 146533 bytes, `cb627101792cc0f8e4b6d99dbdfb466d10b632bf9c1a23aeb70d871bf2c5f3d4` |
| Supplement manifest semantic digest | `238db870df325d11d4724ad2ccdd41287c4da110c11fe328cb62bd2d28bf395d` |
| Bundle location | `..\paper1-v0.4.2-upload` (outside the repository, untracked) |
| Strict verification | passed; every error category empty |

Note that the supplement's byte count and hash differ from the
`12511276...`-bound bundle recorded in the 2026-08-07 audit and handoff
(146528 bytes vs. 146533 bytes). This is expected: the supplement embeds the
packaged commit hash inside `upload_checksums.json`, so a different commit
necessarily produces different supplement bytes even when every source file's
content is unchanged. The PDF bytes and hash are unchanged, because the PDF is
copied verbatim from `output/pdf/paper1_statewise_operator_v0.4.2.pdf` and is
not commit-dependent.

The commit binding inside the supplement was read directly and confirmed:

```text
commit:            1877d6c4fe2fc824a969176ab851807b7a6603d8
status:            PRODUCTION_COMMIT_BOUND
repository_url:    https://github.com/To-marigi/universe-theory-lab
remote_visibility: NOT_VERIFIED_BY_BUILDER
```

`remote_visibility` stays `NOT_VERIFIED_BY_BUILDER` because `1877d6c...` has not
been pushed. The owner must verify remote visibility, or push it, before this
binding can be treated as reflecting a publicly resolvable commit.

## What this approval does not resolve

- **F2, unresolved.** The bound predraft literature gate in
  `verify_paper1_bundle.py` (`ZENODO_PREDRAFT_*`) still targets the
  `2026-08-06T2315Z` gate, cut against a superseded candidate. It must be
  replaced by a freshly cut gate immediately before the first draft-save, per
  the procedure in `reports/v0.4.2_paper1_zenodo_handoff_2026-08-07.md`.
- **F4, unresolved.** No HAL bundle exists.
- **Owner acceptance of the current PDF bytes remains unrecorded** in
  `reports/v0.4.2_paper1_owner_decision_amendment_2026-08-07.md`. The current
  approval is of the source commit, not a statement that the PDF bytes
  themselves have been owner-accepted; the builder and verifier still bind the
  sentence saying that acceptance is missing, and this record does not remove
  that binding.
- **A structural consequence, stated so it is not mistaken for a regression
  later:** cutting the F2 gate and recording the missing owner acceptance both
  require editing files that this bundle's `source_commit_binding` currently
  pins to `1877d6c...` (`UPLOAD_CHECKLIST.md`,
  `reports/v0.4.2_paper1_owner_decision_amendment_2026-08-07.md`,
  `owner_decision.json`, and related supplement members). Completing either one
  will produce a new commit, at which point `1877d6c...` is no longer the final
  source commit and this bundle must be rebuilt again. This is not a failure of
  today's approval; it is the ordinary consequence of closing gates that touch
  bound files. The deposit-ready path is to close F2 and record the owner
  acceptance in the same final commit, obtain owner approval of that commit,
  and rebuild once more from it.

## Superseded records

- `reports/v0.4.2_paper1_final_audit_2026-08-07.md`, "Packaged commit" table
  (bound to `12511276...`).
- `reports/v0.4.2_paper1_zenodo_handoff_2026-08-07.md`, "Verified production
  bundle" table (bound to `12511276...`).

Both are corrected by cross-reference to this record rather than by rewriting
their own dated content, so the 2026-08-07 sequence of events remains legible.
