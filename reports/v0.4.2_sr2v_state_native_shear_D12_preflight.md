# SR2-V state-native shear D12: Q5 and budget preflight

Date: 2026-08-02
Verdict: **`SR2V_STATE_NATIVE_SHEAR_D12_PREFLIGHT_SUPERSEDED_EXECUTION_CANCELLED`**

## Superseding execution decision

This preflight remains a valid provenance certificate for the `Q5`
localisation and the approved resource limits, but its solver launch is
permanently cancelled, and the live certificate now fails closed unless that
cancellation is bound. The later exact certificate
`results/v0.4.2_sr2v_fixed_harmonic_cpobc_obstruction.json` proves that the
complete 262-parameter alpha/beta relation ideal for this fixed harmonic state
assignment already contains `1` before `D12` or `Q5` localisation. Therefore
the declared principal-open relation intersection is empty, no chart budget
should be spent, and the `UNRESOLVED` wording below is retained only as the
historical state at preflight time.

## Result

The previously compiled state-native shear `D12` manifest retains the two
historical pre-solver bindings:

1. supplemental `Q5` nonsingularity is imposed independently by the exact
   localization

   ```text
   detQ5 = Q5:00*Q5:11 - Q5:01*Q5:10,
   sigma:detQ5*detQ5 - 1 = 0;
   ```

2. the campaign is bound by raw SHA-256 to the version-controlled budget
   `config/v0.4.2_sr2v_state_native_shear_D12_budget.json` with the
   owner-approved limits 3,600 seconds per chart, 43,200 seconds total, and
   8 GiB memory.

The determinant is reconstructed over `QQ` without importing the D12
compiler.  It is a primitive two-term polynomial of degree two.  The
coefficient-one monomial `Q5:00*Q5:11` proves it is not the zero polynomial;
independently, `Q5=I` evaluates it to one and satisfies the localization with
`sigma:detQ5=1`.

The existing exact D12 ambient point has

```text
D12 = 155389/220996566,
detQ5 = 1.
```

Therefore the simultaneous ambient principal open `D12!=0, detQ5!=0` is
nonempty.  The point is already known not to solve the required relation
ledger, so this is not a witness.  The later unit-ideal certificate separately
proves that its intersection with the relation variety is empty on this fixed
harmonic slice.

## Fail-closed bindings

The certificate pins:

- D12 manifest raw SHA-256:
  `125d21520df39578e102300c5e0d15509631e1c579506c361fdd58f216312d82`;
- D12 semantic digest:
  `82cd51ddd29e5923b003c8cf8f75a5a26cf5a36edf68728ad58d298d3f573090`;
- campaign budget raw SHA-256:
  `f192a37b97e70c84aeae594db75832bdcf632bf00a78009ff45f09b1bcca2e50`;
- reconstructed `detQ5` digest:
  `81da7f043537e1b7a7943135e03e9e7be3edfb3119ba064243df4e6304256124`;
- superseding fixed-state obstruction raw SHA-256:
  `25abf593a2f8cae1ca75d5d164bc60b1c79327111544c04278621e6b0ed37183`;
- superseding obstruction semantic digest:
  `d5514f31bf6caef5ca102ef8ce78f0bcf137e72922c0f230c716386f22cffc7d`.

A missing budget, an added budget key, any non-approved limit, raw input
drift, semantic drift, a solver-status change, or a promoted witness causes
preflight construction to fail.  There is no inherited/default budget.

## Claim boundary and cancelled operation

No Sage, Gröbner, finite-field, numerical, or other solver was invoked.  The
relation-variety intersection was **`UNRESOLVED`** at preflight time, but it is
now certified empty on this fixed harmonic state slice.  The witness field is
null and execution is **`EXECUTION_CANCELLED_BY_LATER_UNIT_IDEAL`**.  The
137-coordinate double-localized input is retained only as historical
provenance; this certificate does not authorise any later solver run.  The
cancellation is terminal only for this fixed state assignment, not for SR2-V.

Artifact:
`results/v0.4.2_sr2v_state_native_shear_D12_preflight.json`

Semantic digest:
`58b77ba00838a1051364c2c7feebbbe99bf3104b957c15d4e356fe2fdd8aa524`

## Reproduction

```powershell
uv run python -m universe_lab.final_theory.weak_d2_state_native_shear_d12_preflight_v042
uv run pytest -q tests/final_theory/test_weak_d2_state_native_shear_d12_preflight_v042.py
```
