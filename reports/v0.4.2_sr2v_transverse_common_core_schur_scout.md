# SR2-V common-core five-Q Schur scout

Date: 2026-08-02

Verdict:
`SR2V_TRANSVERSE_COMMON_CORE_FIXED4_REJECTED_FULL_M0_NO_ESCAPE_AT_ADVERSARIAL_POINT_OPEN`

Semantic digest:
`b977d2a21444beae5d853680c0945dd74fef1504f4b3045f587c509936b4a35a`

This exact scout starts after the global 127-by-127 non-`Q` unit minor and
works in the Schur columns `(Q1,Q2,Q3,Q4,Q5)`.  It now contains 83 exact
evaluations: the original 81-point selection ledger and two deliberately
chosen exact rational rank-test points.  It is a regression and
candidate-rejection result, not a global row-module proof.

## Original 81-point ledger

The original ledger contains the reference equal point, all 49 single
primitive-kernel directions, and all 31 nonempty supports of
`{K12,K13,K15,K44,K45}`.  The second fixed-four selection

```text
{23,147,154,688}
```

contains the star `c12,c13,c14` at all 81 points.  Its rank census
`(candidate,star,augmented)` is

```text
0/0/0 : 37
1/0/1 :  3
2/1/2 :  5
3/1/3 :  3
3/3/3 :  2
4/1/4 :  2
4/3/4 : 29
```

On these 81 points only, four is minimal.  Filtering all 1,127 common-core
rows through the 31 star-rank-three points leaves `{29,147,548}` as the only
possible fixed triple, and that triple fails 41 points.  This has no global
minimality meaning.

## Point 82: rejection of the second fixed four

At the exact rational slice point

```text
(u12,u13,u15,u44,u45)=(2,3,5,21,7)
```

Direct reconstruction gives ranks `3/3/4`, so `{23,147,154,688}` is rejected.
The artifact certifies this pointwise rank calculation; it does not certify a
polynomial factor or a locus containing the point.  The next selection

```text
{23,31,147,688}
```

has rank 4 there and passes the preceding 82-point ledger.  Over the original
81 points its rank census is

```text
0/0/0 : 39
1/0/1 :  1
2/1/2 :  5
3/1/3 :  3
3/3/3 :  2
4/1/4 :  2
4/3/4 : 29
```

## Point 83: direct positive-rational rejection of the third fixed four

At the stored positive rational slice point

```text
(u12,u13,u15,u44,u45)=(1/2,1,1,1,2)
```

The candidate and star Schur rows reconstruct directly as

```text
candidate:
(5/192, 0,    0,    0,    0)
(1/32,  0,   -1/4,  0,    0)
(1/82,  0,    0,   -1/4,  0)
(0,     0,    0,    0,    0)

star c12,c13,c14:
(0,     -1/8, 0,    0,    0)
(1/64,   0,  -1/8,  0,    0)
(1/164,  0,   0,   -1/8,  0)
```

Thus the ranks are again `3/3/4`.  The exact escape is `e_Q2`: all four
candidate rows evaluate to zero, while the star evaluates to `(-1/8,0,0)`.
Adding row 154 gives the residual

```text
(19/1640,153/3280,-32/205,-1/4,0)
```

and restores rank 4 at this point only.  This is not evidence for a global
fixed-five selection.

## Full-module control

At both adversarial points,

```text
rank(M0)=rank(M0+star)=rank(M0+all six commutators)=131.
```

Each of the four literal/derived Eq. (113) and strict/completed Eq. (139)
branches also remains `131 -> 131`.  The adversarial points therefore reject
two finite row selections without producing a noncommutative witness or
damaging the global 127-column unit minor.

## Methodological conclusion

Finite direction ledgers and isolated exact points can select or reject row
sets, but cannot certify a multivariate Schur module.  The active route is the
full `M0` Schur module, using localized identities and exact cover
certificates on the Eq. (120) `g`-charts rather than another global
fixed-four guess.

## Reproduction

```powershell
uv run python -m universe_lab.final_theory.sr2v_transverse_common_core_schur_scout_v042
uv run pytest -q tests/final_theory/test_sr2v_transverse_common_core_schur_scout_v042.py
```

Expected focused result: `9 passed`.

## Claim boundary

All stored ranks, residuals, and escape evaluations are exact over `QQ`.  The
83 evaluations reject the recorded fixed row selections but do not certify a
determinant polynomial, degree, term count, factorization, or algebraic locus.
They do not prove or refute global common-core star membership and do not issue
an SR2-V terminal.
