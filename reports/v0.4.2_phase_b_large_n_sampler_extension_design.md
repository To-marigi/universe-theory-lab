# Phase-B large-N sampler extension design

Status: **`PHASE_B_LARGE_N_SAMPLER_EXTENSION_DESIGN_FROZEN_IMPLEMENTATION_PREFLIGHT_BUDGET_REQUIRED`**

Semantic digest: `9aa245a4936251ff5b60fdfbafbe06c92e0a215a3a46bd23cb36ec0a2a8fa953`

## Decision

The existing exact kernel cannot scale by enumerating every unlabeled
poset and every vertex permutation. The frozen local rule can instead
be evaluated on a naturally birth-labeled history by assigning exact
rational weight to every down-set precursor. Summing labeled choices
over an automorphism orbit reproduces the frozen orbit multiplicity.

This is a design obligation, not yet an implementation certificate.
Exact down-set enumeration remains exponential in the worst case and
must fail closed at an owner-approved cap.
The current exhaustive height and width helpers are also forbidden
on the large-N path; height must use an exact longest-path dynamic
program verified against the bounded oracle.
The geometric dimension-four control and BDG action evaluation are
separate; no BDG-weighted sampling ensemble is currently defined.

## Production blockers mapped

- large-N candidate and negative-control sampler: `primary_exact_labeled_sampler, large_n_observable_extension, approximate_weighted_ideal_route`
- fixed production spectral plateau window: `spectral_protocol_extension`
- executable BDG dimension-four ensemble: `positive_control_extension`
- independent replay implementation: `independent_replay_design`
- hard wall-time and memory supervision: `resource_supervision_design`
- versioned production measurement budget: `resource_supervision_design, bounded_implementation_preflight`

## Acceptance checks

| check | passed |
|---|---:|
| `predecessor_preflight_is_complete_but_not_production_validated` | `True` |
| `frozen_candidate_weights_are_unchanged` | `True` |
| `current_exact_kernel_bottleneck_is_recorded` | `True` |
| `labeled_quotient_route_preserves_frozen_semantics_by_design` | `True` |
| `hidden_exponential_observable_path_is_recorded_and_replaced_by_design` | `True` |
| `approximate_route_cannot_silently_replace_exact_sampling` | `True` |
| `every_preflight_blocker_has_a_design_owner` | `True` |
| `independent_replay_and_control_leakage_are_fail_closed` | `True` |
| `production_statistics_and_claims_remain_unfrozen_and_bounded` | `True` |
| `implementation_requires_a_new_human_owned_budget` | `True` |
| `no_sampling_solver_or_scientific_authorization_is_added` | `True` |

## Boundary

No new trajectory, approximate sampler, BDG ensemble, production
measurement, solver run, or scientific verdict is authorized. The
next step requires a new human-owned resource budget.

Next gate: `PHASE_B_LABELED_SAMPLER_AND_OBSERVABLE_EQUIVALENCE_COST_PREFLIGHT_BUDGET_APPROVAL`
